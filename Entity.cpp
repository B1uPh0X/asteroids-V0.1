
#include "Entity.h"
#include <math.h>

void Entity::move(){
    int x, y;
    double rot, vel = 0.0;

    x = vel * cos(rot);
    y = vel * sin(rot);
}

bool Entity::isalive(){
    if(
}

void Entity::spawn(sf::RenderWindow &window){

}